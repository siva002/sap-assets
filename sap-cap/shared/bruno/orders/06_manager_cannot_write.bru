meta {
  name: Manager write → 403
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/Orders(ID={{draftOrderId}},IsActiveEntity=true)
  body: json
  auth: basic
}

auth:basic {
  username: {{managerUser}}
  password: password
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "notes": "Manager trying to edit — should be blocked"
  }
}
