meta {
  name: Approve order (Manager)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/Orders(ID={{submittedOrderId}},IsActiveEntity=true)/OrderService.approve
  body: none
  auth: basic
}

auth:basic {
  username: {{managerUser}}
  password: password
}

headers {
  Accept: application/json
}
