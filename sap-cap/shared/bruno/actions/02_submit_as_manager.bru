meta {
  name: Submit as Manager → 403
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/Orders(ID={{draftOrderId}},IsActiveEntity=true)/OrderService.submit
  body: none
  auth: basic
}

auth:basic {
  username: {{managerUser}}
  password: password
}

headers {
  Accept: application/json
}
